import { ScrollnavSectionDirective } from './scrollnav-section.directive';

describe('ScrollnavSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollnavSectionDirective();
    expect(directive).toBeTruthy();
  });
});
