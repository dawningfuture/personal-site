import { ScrollnavManagerDirective } from './scrollnav-manager.directive';

describe('ScrollnavManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollnavManagerDirective();
    expect(directive).toBeTruthy();
  });
});
